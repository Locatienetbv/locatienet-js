export type MapMapOptions = {
    /**
     * The language of texts on the rendered map. Defaults to nl. This standard allows to use two-letter ISO 639-1 codes
     */
    language?: string | null;
};
