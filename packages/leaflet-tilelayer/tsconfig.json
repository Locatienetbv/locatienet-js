{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "rootDir": "src",
        "outDir": "dist"
    }, 
    "include": [ "src", "../../shared/src"],
    "exclude": [ "dist" ] // Ignore compiled JS and dependencies
}
